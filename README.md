# CloudTasker – Flask + MySQL (Dockerized App)

A simple Flask web application with a MySQL backend, containerized using Docker and Docker Compose.  
Created by **Sahil Pal** to demonstrate cloud and DevOps fundamentals.

### How to Run
```bash
docker compose up --build

